import { ConsumerIdentityInput } from '../awsEvents/AwsInputEvent';
import { CustomField } from '../customField/CustomField';
import { AccessToken } from './AccessToken';
import { BasicChannelInfo } from '../channel/ChannelInfo';
export interface ConsumerAuthorizationSuccessPayloadData {
    accessToken?: AccessToken;
    consumerIdentity: ConsumerIdentityInput;
    channel?: BasicChannelInfo;
    contact?: {
        customFields: Array<CustomField>;
    };
    customer?: {
        customFields: Array<CustomField>;
    };
}
