import { SessionId } from '../post/session/SessionId';
export declare type ChatSessionAssigningPayload = {
    sessionId: SessionId;
    queue: number | null;
    isAnyAgentOnlineForQueue: boolean;
};
