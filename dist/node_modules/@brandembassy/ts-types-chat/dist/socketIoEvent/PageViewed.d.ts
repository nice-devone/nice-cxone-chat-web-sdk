import { SessionId } from '../post/session/SessionId';
export declare type Page = {
    id: string;
    pageUrl: string;
    pageTitle: string;
    visitedAt: string;
    hasHappenedDuringSession: boolean;
    isOpeningVisit: boolean;
};
export declare type PageViewedPayload = {
    pages: Array<Page>;
    sessionId: SessionId;
};
