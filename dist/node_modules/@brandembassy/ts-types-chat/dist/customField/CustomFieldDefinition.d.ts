export declare type CustomFieldDefinitionPossibleValue = {
    name: string;
    value: string;
    parentId?: string;
};
export declare enum CustomFieldType {
    TEXT = "text",
    EMAIL = "email",
    LIST = "list",
    TREE = "tree"
}
export declare type CustomFieldDefinition = {
    ident: string;
    label: string;
    type: CustomFieldType;
    required: boolean;
    values: Array<CustomFieldDefinitionPossibleValue>;
    liveChatVisibility: 'visible' | 'hidden';
    postDetailVisibility: 'visible' | 'hidden';
    isEditable: boolean;
};
