import * as yup from 'yup';
import { ChatWindowId } from '../../chatWindow/ChatWindowId';
export declare const destinationSchema: yup.ObjectSchema<{
    id: string;
}>;
export declare type Destination = yup.InferType<typeof destinationSchema> & {
    id: ChatWindowId;
};
