import * as yup from 'yup';
import { Override } from '../utils/override';
import { MessageType } from './MessageType';
export declare const messageContentSchema: yup.ObjectSchema<{
    text: string;
    type: string;
    payload: {
        text: any;
        postback: any;
        elements: any;
    };
    fallbackText: string;
    isAutoTranslated: boolean;
}>;
export declare type MessageContent = Override<yup.InferType<typeof messageContentSchema>, {
    type: MessageType;
}>;
