import { EndUserIdentityId } from './EndUserIdentityId';
import * as yup from 'yup';
export declare const endUserSchema: yup.ObjectSchema<{
    id: string;
    firstName: string;
    surname: string;
}>;
export declare type EndUser = yup.InferType<typeof endUserSchema> & {
    id: EndUserIdentityId;
};
