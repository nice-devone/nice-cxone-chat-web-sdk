import * as yup from 'yup';
import { Override } from '../utils/override';
import { QuickReplyId } from './QuickReplyId';
export declare const quickReplySchema: yup.ObjectSchema<{
    id: string;
    title: string;
    content: string;
    dynamicContentUrl: string;
    isDynamicContent: boolean;
    hasRichText: boolean;
}>;
export declare type QuickReplyDeprecated = Override<yup.InferType<typeof quickReplySchema>, {
    id: QuickReplyId;
}>;
